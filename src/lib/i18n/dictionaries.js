// Import the comprehensive dictionaries from the new modular system
import dictionaries, { getDictionary, languages } from './index.js';

// Re-export for backward compatibility
export default dictionaries;
export { getDictionary, languages };
